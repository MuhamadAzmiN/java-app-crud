<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.Datasarana" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Warna tema utama -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/secondary_blue</item>
        <item name="colorOnSecondary">@color/on_primary</item>

        <!-- Warna tombol -->
        <item name="buttonStyle">@style/BlueButton</item>
    </style>

    <style name="Theme.Datasarana" parent="Base.Theme.Datasarana" />

    <!-- Opsional: style untuk button agar biru -->
    <style name="BlueButton" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/primary_blue</item>
        <item name="android:textColor">@color/on_primary</item>
    </style>

</resources>
